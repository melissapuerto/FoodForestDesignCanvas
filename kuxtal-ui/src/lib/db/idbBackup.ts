/**
 * DEPRECATED / UNUSED.
 *
 * IndexedDB persistence is implemented directly in `sqlite.ts` (the in-memory +
 * IndexedDB `persistence==='idb'` path). This module is intentionally empty to
 * avoid a second, competing implementation. Safe to delete.
 */
export {};
